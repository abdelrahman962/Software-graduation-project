import 'package:flutter/material.dart';

class OwnerInvoiceReportsScreen extends StatelessWidget {
  const OwnerInvoiceReportsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Invoice Reports')),
      body: const Center(child: Text('Invoice Reports Screen - Coming Soon')),
    );
  }
}
